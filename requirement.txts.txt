# Core Django
Django==5.1.6
asgiref==3.8.1
sqlparse==0.4.4

# Database
psycopg2-binary==2.9.9  # For PostgreSQL (recommended for production)
# or use mysqlclient if using MySQL

# Static Files
whitenoise==6.6.0  # For static file serving

# Production WSGI Server
gunicorn==21.2.0

# AWS Specific
boto3==1.34.38  # For AWS services (S3, etc.)
django-storages==1.14.2  # For S3 file storage

# Additional Dependencies
crispy-forms==2.1
crispy-bootstrap4==2023.10  # If using Bootstrap4 forms
pillow==10.2.0  # For image processing if needed

# Security
python-dotenv==1.0.1  # For environment variables
django-environ==0.11.2  # Alternative for env management

# Monitoring (optional)
sentry-sdk==1.40.6  # Error tracking